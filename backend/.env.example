# Copy to .env and restart uvicorn
DEEPFAKE_DEMO_MODE=0

# Optional: pretrained HuggingFace detector for arbitrary internet videos
# (downloads ~300 MB on first request; better out-of-distribution than SDFVD-only training)
# DEEPFAKE_USE_PRETRAINED_HF=1
